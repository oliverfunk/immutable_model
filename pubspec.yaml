name: immutable_model
description: A tool built upon Equtable and built_collection to help you create complex immutable data models quickly and easily, with validated updates.
version: 0.3.0
homepage: https://github.com/oliverfunk/immutable_model

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  built_collection: ^4.3.2
  equatable: ^1.2.0
  meta: ^1.1.8
  crypto: ^2.1.5

dev_dependencies:
  test:
